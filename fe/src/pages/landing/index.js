import React from 'react';
import Container from '../../components/layout/container';
import Heading from '../../components/particles/heading';

const Landing = () => {
    return <Container>
        <Heading level={3} size={8}>Daily Jobs</Heading>
    </Container>
}

export default Landing;